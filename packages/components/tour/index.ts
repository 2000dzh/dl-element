import { withInstall } from "@dl-element/utils";
import Tour from "./tour.vue";
import TourStep from "./step.vue";

export const DlTour = withInstall(Tour);
export const DlTourStep = withInstall(TourStep);
